(setq meltbus-theme-packages
      '((meltbus-theme :location (recipe :fetcher local))
        (meltbus-light-theme :location (recipe :fetcher local))))

(defun meltbus-theme/init-meltbus-theme ()
  nil)

(defun meltbus-theme/init-meltbus-light-theme ()
  nil)
